# Server Configuration
PORT=

# CHANGE: No database URLs needed - gateway only routes GraphQL requests
# Service GraphQL endpoints (unchanged - gateway still uses GraphQL)
# Service base URLs (no /graphql suffix needed as gateway adds it in executor)
AUTH_SERVICE_URL=
PRODUCT_SERVICE_URL=
ORDER_SERVICE_URL=
AI_SERVICE_URL=
INTERNAL_JWT_SECRET=
JWT_SECRET=
GATEWAY_REDIS_URL=

